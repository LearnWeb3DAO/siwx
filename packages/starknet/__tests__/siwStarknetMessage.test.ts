import { ec } from "starknet";

describe("Sign in with Solana", () => {
  test("placeholder", async () => {
    const randomAccount = ec.starkCurve.utils.randomPrivateKey();
    const pubKey = ec.starkCurve.getPublicKey();
  });
});
